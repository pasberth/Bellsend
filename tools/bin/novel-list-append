#!/usr/bin/env sh
TOOLS=$(dirname $(dirname $0))
SRC=`dirname $TOOLS`/source
NOV=`dirname $TOOLS`/novel
NOVPREFIX=${NOVPREFIX%%/}/
NOVPREFIX=${NOVPREFIX##/}

for id in $*
do
  touch $SRC/$NOVPREFIX$id
  git add $SRC/$NOVPREFIX$id
  echo - $id >> $SRC/${NOVPREFIX}config/novel-list.yml
done
